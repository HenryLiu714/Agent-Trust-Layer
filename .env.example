# Copy to .env and `source` it, or export these in your shell. irimi reads only real
# environment variables; it does not load this file.

# Root directory for irimi state (CA now; maps and traces in later versions).
# Default: ~/.irimi
# IRIMI_HOME=~/.irimi
